<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE struts PUBLIC
    "-//Apache Software Foundation//DTD Struts Configuration 2.3//EN"
    "http://struts.apache.org/dtds/struts-2.3.dtd">

<struts>

	<!-- 开发模式设置开始 -->
	<!-- 开发状态 -->
	<constant name="struts.devMode" value="true" />
	<!-- 国际化资源重新加载 -->
	<constant name="struts.i18n.reload" value="true" />
	<!-- 配置文件重新加载 -->
	<constant name="struts.configuration.xml.reload" value="true" />
	<!-- convention类从新加载 -->
	<constant name="struts.convention.classes.reload" value="true" />
	<!-- 开发模式结束 -->

	<!-- 扩展 -->
	<constant name="struts.action.extension" value="action,do" />

	<!-- action后缀。比如为HelloWorldAction。按照配置，actionName为hello_world。 -->
	<constant name="struts.convention.action.suffix" value="Action" />

	<!-- 确定搜索包的路径。只要是包含action的包都要搜索。 basePackage按照默认不用配置，如果配置，只会找以此配置开头的包。 locators及locators.basePackage都是一组以逗号分割的字符串。 -->
	<constant name="struts.convention.package.locators.basePackage" value="com.roc.cxf.action" />
	<constant name="struts.convention.package.locators" value="action" />
	<!-- 禁用包搜索 -->
	<!-- <constant name="struts.convention.package.locators.disable" value="false" /> -->
	<!-- 排除的包。排除哪些包不搜索。按默认配置即可。逗号分割字符串。 -->
	<constant name="struts.convention.exclude.packages"
		value="com.roc.ssh2.demo.action.*,org.apache.struts2.*" />

	<!-- <include file="struts-default.xml" /> -->

	<!-- <package name="person" extends="struts-default" namespace="/">
		<action name="*" method="{1}" class="personAction">
			<result name="success">/person.jsp</result>
		</action>
	</package> -->
	
</struts>